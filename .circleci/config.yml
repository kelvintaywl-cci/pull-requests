version: 2.1

jobs:
  echo:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - run:
          name: Inspect
          command: echo "I love ${COLOR} builds."
          environment:
            COLOR: "green"

workflows:
  main:
    jobs:
      - echo
